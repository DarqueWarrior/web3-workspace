services:
  app:
    entrypoint:
    - sleep
    - infinity
    image: darquewarrior/ethereum:0.1.2
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock

